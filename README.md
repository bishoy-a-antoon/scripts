# scripts

- (ghfetch)[ghfetch]: Fetch git repos by programming languages and keywords
- (psh)[psh]: List procses and sort them by memory usage
- (aptfind)[aptfind]: Fetch apt packages by keywords and format them in a more user-friendly way
